// <auto-generated>
// Generated by figma-flutter-agent. Do not edit by hand.
// </auto-generated>

import 'package:flutter/material.dart';
import 'package:{{ package_name }}/theme/app_colors.dart';
import 'package:{{ package_name }}/theme/app_spacing.dart';
import 'package:{{ package_name }}/theme/app_typography.dart';
{% if radii %}import 'package:{{ package_name }}/theme/app_radius.dart';
{% endif %}{% if elevations %}import 'package:{{ package_name }}/theme/app_elevation.dart';
{% endif %}{% if edge_insets %}import 'package:{{ package_name }}/theme/app_edge_insets.dart';
{% endif %}

// <custom-code>
// </custom-code>

/// Living style guide generated from Figma design tokens.
class DesignGalleryScreen extends StatelessWidget {
  const DesignGalleryScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Design tokens')),
      body: ListView(
        padding: const EdgeInsets.all(16),
        children: [
{% if colors %}
          _sectionTitle('Colors'),
          Wrap(
            spacing: AppSpacing.sm,
            runSpacing: AppSpacing.sm,
            children: [
{% for color in colors %}
              _colorSwatch('{{ color.name }}', AppColors.{{ color.name }}),
{% endfor %}
            ],
          ),
{% endif %}
{% if typography %}
          _sectionTitle('Typography'),
{% for style in typography %}
          Padding(
            padding: const EdgeInsets.only(bottom: 8),
            child: Text(
              '{{ style.style_name }} — {{ style.font_size }} / {{ style.font_weight }}',
              style: AppTypography.{{ style.style_name }},
            ),
          ),
{% endfor %}
{% endif %}
{% if spacing %}
          _sectionTitle('Spacing'),
{% for space in spacing %}
          Text('{{ space.name }}: {{ space.value }}'),
{% endfor %}
{% endif %}
{% if radii %}
          _sectionTitle('Radii'),
{% for radius in radii %}
          Text('{{ radius.name }}: {{ radius.value }}'),
{% endfor %}
{% endif %}
{% if elevations %}
          _sectionTitle('Elevations'),
{% for elevation in elevations %}
          Text('{{ elevation.name }}: {{ elevation.value }}'),
{% endfor %}
{% endif %}
{% if edge_insets %}
          _sectionTitle('Edge insets'),
{% for inset in edge_insets %}
          Text('{{ inset.name }}: L{{ inset.left }} T{{ inset.top }} R{{ inset.right }} B{{ inset.bottom }}'),
{% endfor %}
{% endif %}
{% if icons %}
          _sectionTitle('Icons'),
{% for icon in icons %}
          Text('{{ icon.name }} → assets/{{ icon.asset_key }}'),
{% endfor %}
{% endif %}
        ],
      ),
    );
  }

  static Widget _sectionTitle(String label) {
    return Padding(
      padding: const EdgeInsets.only(top: 24, bottom: 8),
      child: Text(label, style: const TextStyle(fontSize: 18, fontWeight: FontWeight.w600)),
    );
  }

  static Widget _colorSwatch(String name, Color color) {
    return Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        Container(width: 48, height: 48, color: color),
        const SizedBox(height: 4),
        Text(name, style: const TextStyle(fontSize: 11)),
      ],
    );
  }
}
