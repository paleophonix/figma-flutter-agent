// Generated by figma-flutter-agent. Update goldens: flutter test test/golden --update-goldens



import 'package:flutter/material.dart';

import 'package:flutter_test/flutter_test.dart';

import 'package:{{ package_name }}/theme/app_theme.dart';



void main() {
{% for specimen in specimens %}
  testWidgets('{{ specimen.id }} typography specimen', (tester) async {
    await tester.binding.setSurfaceSize(const Size({{ specimen.width }}, {{ specimen.height }}));
    await tester.pumpWidget(
      MaterialApp(
        theme: AppTheme.light(maxWebWidth: {{ max_web_width }}),
        home: Scaffold(
          body: Center(
            child: SizedBox(
              width: {{ specimen.width }},
              height: {{ specimen.height }},
              child: Text(
                '{{ specimen.text | replace("'", "\\'") }}',
                textAlign: TextAlign.center,
                style: TextStyle(
                  fontFamily: '{{ specimen.pubspec_family }}',
                  fontSize: {{ specimen.font_size }},
                  fontWeight: FontWeight.{{ specimen.font_weight }},
                  {% if specimen.line_height is not none %}height: {{ specimen.line_height }},{% endif %}
                  {% if specimen.letter_spacing is not none %}letterSpacing: {{ specimen.letter_spacing }},{% endif %}
                  leadingDistribution: TextLeadingDistribution.proportional,
                ),
              ),
            ),
          ),
        ),
      ),
    );
    await tester.pumpAndSettle();

    await expectLater(
      find.byType(Text),
      matchesGoldenFile('goldens/{{ specimen.id }}.png'),
    );
  });
{% endfor %}
}
