// <auto-generated>
// Generated by figma-flutter-agent. Do not edit by hand.
// </auto-generated>

import 'package:flutter/material.dart';

// <custom-code>
// </custom-code>

/// Responsive breakpoints aligned with spec §7.3.
class AppBreakpoints {
  AppBreakpoints._();

  /// Mobile (compact): 320–480
  static const double mobileSmallMax = 480;
  /// Mobile (large): 481–768
  static const double mobileLargeMax = 768;
  /// Tablet: 769–1024
  static const double tabletMax = 1024;

  static bool isMobileSmall(double width) => width <= mobileSmallMax;
  static bool isMobileLarge(double width) =>
      width > mobileSmallMax && width <= mobileLargeMax;
  static bool isMobile(double width) => width <= mobileLargeMax;
  static bool isTablet(double width) =>
      width > mobileLargeMax && width <= tabletMax;
  static bool isDesktop(double width) => width > tabletMax;

  /// Mobile-large, tablet, and desktop: column reflow and denser grids (spec §7.3).
  static bool isWideLayout(double width) => width > mobileSmallMax;

  static double horizontalPadding(double width) {
    if (isMobileSmall(width)) {
      return 16;
    }
    if (isMobileLarge(width)) {
      return 20;
    }
    if (isTablet(width)) {
      return 24;
    }
    return 32;
  }

  static double contentMaxWidth(double width, double configuredMaxWebWidth) {
    if (isMobile(width)) {
      return width;
    }
    return configuredMaxWebWidth;
  }
}

class AppLayoutExtension extends ThemeExtension<AppLayoutExtension> {
  const AppLayoutExtension({required this.maxWebWidth});

  final double maxWebWidth;

  @override
  AppLayoutExtension copyWith({double? maxWebWidth}) {
    return AppLayoutExtension(maxWebWidth: maxWebWidth ?? this.maxWebWidth);
  }

  @override
  AppLayoutExtension lerp(ThemeExtension<AppLayoutExtension>? other, double t) {
    if (other is! AppLayoutExtension) {
      return this;
    }
    return AppLayoutExtension(
      maxWebWidth: maxWebWidth + (other.maxWebWidth - maxWebWidth) * t,
    );
  }
}
