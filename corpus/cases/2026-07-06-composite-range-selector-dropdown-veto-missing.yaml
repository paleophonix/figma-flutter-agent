version: 1

case:
  id: 2026-07-06-composite-range-selector-dropdown-veto-missing
  title: Composite range selector misclassified as dropdown with leaked frame names
  case_kind: regression
  project: test
  feature: rep_edit_profile
  observed_at: 2026-07-06
  created_at: '2026-07-06T14:05:00Z'
  updated_at: '2026-07-06T15:35:00Z'
  summary: >
    The commission range selector (node 2237:17339) is a composite control:
    label plus two paired numeric inputs, a unit suffix and a range slider. The
    arbiter accepted input_dropdown (conf 0.9) without a veto, and the emitter
    rendered a native DropdownButton whose menu items are the raw child frame
    names ("Frame 3764", "Frame 3763"). A structural composite is collapsed into
    a single-select control and the item labels leak Figma frame identifiers.
    User observation: "выпадающий список" appears on a control that is not a picker.

occurrences:
  - id: rep-edit-profile-commission-dropdown-veto
    family_id: semantic_veto_not_applied
    pipeline_arrow: A3
    law_id: LAW-A3-CLASSIFY
    stage: classification
    origin: COMPILER
    blast_radius: B2_SILENT_WRONG_BEHAVIOR
    confidence: high
    status: FIXED
    owner:
      module: src/figma_flutter_agent/generator/layout/widgets/emit/containers.py
      symbol: render_container_body
    contract:
      field: semanticKind (input_dropdown on composite range selector)
      field_class: proposal
      category: illegal
      expected: >
        A multi-child composite (paired inputs + slider) must not win a
        single-select dropdown kind; the arbiter vetoes or abstains and the
        subtree materializes structurally.
      actual: >
        input_dropdown accepted and emitted as DropdownButton with menu items
        Text('Frame 3764') / Text('Frame 3763') sourced from child frame names.
    authority_boundary: semantic_manifest
    evidence:
      - kind: source_code
        path: apps/test/lib/widgets/commission_range_selector_widget.dart
        summary: DropdownButton<String> items are DropdownMenuItem(child Text('Frame 3764'/'Frame 3763')).
      - kind: debug_artifact
        path: .debug/screen/test/rep_edit_profile/semantics.json
        summary: accepted bucket contains input_dropdown (conf 0.9) for the composite selector node.
    repair:
      summary: >
        Share layout_fact_composite_dropdown_host between arbiter veto and emitter;
        when a DROPDOWN-typed node is a composite multi-input host, skip
        render_dropdown and fall through to structural emit so frame names never
        become DropdownMenuItem labels.
      changed_files:
        - src/figma_flutter_agent/parser/interaction/forms.py
        - src/figma_flutter_agent/parser/semantics/arbiter.py
        - src/figma_flutter_agent/generator/layout/widgets/emit/containers.py
      regression_tests:
        - tests/test_rep_edit_profile_batch_repair.py::test_composite_dropdown_host_emits_without_dropdown_button
        - tests/test_rep_edit_profile_batch_repair.py::test_composite_dropdown_veto_rejects_multi_input_host
      verification:
        - poetry run pytest -q tests/test_rep_edit_profile_batch_repair.py -k composite_dropdown
