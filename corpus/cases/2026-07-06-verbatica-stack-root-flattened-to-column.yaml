version: 1

case:
  id: 2026-07-06-verbatica-stack-root-flattened-to-column
  title: Paywall STACK root lowered to Column with full-height wallpaper lead
  case_kind: regression
  project: test
  feature: verbatica_paywall
  observed_at: 2026-07-06
  created_at: '2026-07-06T17:20:00Z'
  updated_at: '2026-07-06T17:20:00Z'
  summary: >
    LAW-ARTBOARD-WALLPAPER-PRESERVES-STACK — geometry-planner decomposed STACK
    root with ambient wallpaper used stack_should_flow_as_column and inserted
    _buildBackground as a 780px Column lead, pushing content off-screen.

occurrences:
  - id: verbatica-paywall-stack-column-flatten
    family_id: stack_root_flattened_to_column
    pipeline_arrow: A3
    law_id: LAW-ARTBOARD-WALLPAPER-PRESERVES-STACK
    stage: emitter
    origin: COMPILER
    blast_radius: B3_BLOCKING
    confidence: high
    status: FIXED
    owner:
      module: src/figma_flutter_agent/generator/layout/file_methods.py
      symbol: compose_decomposed_root_widget
    contract:
      field: stack_root_materialization
      field_class: intent
      category: illegal
      expected: >
        STACK artboard roots with extracted wallpaper backgrounds must compose as
        Stack overlay (background layer + positioned content), not Column flow
        with full-height wallpaper as first sibling.
      actual: >
        Column(mainAxisSize.min, [SizedBox(height:780, _buildBackground), ...content])
        for root 2399:42778 STACK with absolute Img layer 2399:42779.
    authority_boundary: emitter
    evidence:
      - kind: debug_artifact
        path: .debug/screen/test/verbatica_paywall/processed.json
        summary: Root 2399:42778 type STACK; Img child has stackPlacement absolute.
      - kind: debug_artifact
        path: apps/test/lib/generated/verbatica_paywall_layout.dart
        summary: >
          Default browser branch Column lists SizedBox(height:780, _buildBackground)
          before feature builders; 419px bottom overflow in capture.
    repair:
      summary: >
        Veto stack_should_flow_as_column when artboard_background_lead is set so
        wallpaper paints inside Stack children, not as a column-flow lead.
      changed_files:
        - src/figma_flutter_agent/generator/layout/file_methods.py
      regression_tests:
        - tests/test_layout_flex_policy.py::test_artboard_wallpaper_lead_preserves_stack_root_not_column_flow
      verification:
        - poetry run pytest tests/test_layout_flex_policy.py::test_artboard_wallpaper_lead_preserves_stack_root_not_column_flow -q
