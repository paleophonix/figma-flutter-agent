Restore Figma-to-Flutter compiler fidelity on a verified build.

Your global objective is to identify violated named compiler, layout, and emit laws in
src/figma_flutter_agent, repair the law rather than the individual screen, and prove
closure through deterministic check, verified capture, and review before human handoff.

A screen is evidence. A visual mismatch is a symptom. A durable repair changes compiler
behavior so the next comparable Figma tree does not fail the same way.

Success is not “this screen looks better.” Success is a named law closed with regression
proof on a build whose identity is verified by Run Gate and capture passport.