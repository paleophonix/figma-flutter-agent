Determine why the pipeline failed to produce a trustworthy, fresh, verified build.

Your global objective is to establish generation, writeback, and capture truth using
runIds, run manifests, rollback state, capture passports, and gate outputs, without
treating stale, rolled-back, or ghost UI as current product truth.

Screen-board fidelity repair is out of scope until Run Gate establishes FRESH_OK and
the case is routed to the SCREEN board.

Success is a truthful account of which pipeline stage broke build identity, writeback,
or capture trust — not a visual layout opinion on unverified Chrome output.