Work from recognise.symptoms for pipeline-trust failures. Do not re-run recognise and do not invent visual layout symptoms.

FORENSIC inspect maps pipeline truth failures, not screen fidelity. Answers WHERE in the pipeline, not WHY the law failed.

Inspect may say: this symptom touches writeback rollback handling and Run Gate build identity comparison. Inspect may not say: fix write.py by patching one screen.

Do not emit lawId, repairShape, proposed fixes, implementation steps, or screen compiler blame for visual misalignment.

Do not inspect figma.png, capture screenshots, compare strips, heatmaps, or vision bundle images for visual layout symptoms.

Prioritise run_manifest.json, last.log stage markers, dart-errors.json, capture.json, snapshot.json, candidate artifact availability, writeback state, served runId probe results, and rollback evidence.

Repo read is allowed but must follow symptom anchors and inspect_preflight. Do not grep the whole compiler tree for UI emit modules on the forensic board.

Repo navigation map in L6 provides forensicSurfaces only. It is navigation aid, not evidence. Do not cite the map as proof.

Every entity must link to at least one recognise symptom id. Every entity must include artifactRefs and repoPaths when the pipeline surface is known, or blocked=true with missing_evidence.

One entity represents one pipeline concern: write, analyze, capture, serve, run_gate, rollback, snapshot, toolchain, or orchestrator.

Use artifactRefs for manifest paths and log stage markers. Do not use visual diff scores to invent screen entities.

On diagnose.refine, do not restart inspect unless the orchestrator explicitly routes inspect.refine.

Do not mutate the cumulative reasoning_chain; write only executive JSON to output_path.
