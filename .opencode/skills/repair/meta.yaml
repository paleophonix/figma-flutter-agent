step: repair
index: 5
mode: build
boards:
  - screen
  - forensic
output_schema: schemas/repair.schema.json
output_path: .repair/state/repair.json
detail_log_path: .repair/state/repair.detail.log
contract: |
  input: plan.steps[] with actionKind CODE_CHANGE only + assigned planStepOrders + run_context
  output: filesTouched + planStepOrders + gates + diffStat
  forbidden: diagnose, replan, non-CODE_CHANGE steps, full generate, capture, golden update, generated Dart edits, loop ownership
