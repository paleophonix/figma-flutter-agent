# Inspect step — SCREEN board

## Purpose

Step 2/7 symptom-driven cartography: recognise.symptoms → artifactRefs → repoPaths. First step with read-only repo access, bounded by anchors.

## Usage example

Orchestrator builds inspect_preflight and repo map slice, assembles prompt with board=screen and repair-master-screen.md L1, validates inspect.json gate before diagnose.

## LLM context

No vision. No laws. WHERE-summary only. L6 includes repo map (navigation only, not evidence). See docs/projects/repair/opencode-debug-state.md triad recognise → inspect → diagnose.
