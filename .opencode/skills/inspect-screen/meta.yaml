step: inspect
index: 2
mode: plan
board: screen
boards:
  - screen
requires_verified_capture: true
allows_repo_read: true
output_schema: schemas/inspect.schema.json
output_path: .repair/state/inspect.json
detail_log_path: .repair/state/inspect.detail.log
contract: |
  input: recognise.symptoms + inspect_preflight + run_context + reasoning_chain
  output: entities[] (symptom → artifactRefs → repoPaths WHERE map)
  forbidden: vision, laws, root cause, fixes, repairShape, targetFiles, free repo grep
