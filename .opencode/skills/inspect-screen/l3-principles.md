Work from recognise.symptoms. Do not re-run recognise and do not invent new visual symptoms unless you note them for recognise.refine in detail log only.

Inspect answers WHERE, not WHY. Entity summaries must identify artifact and code surfaces, not causal laws or fixes.

Inspect may say: this symptom touches generated shell layout and common layout emitter surfaces. Inspect may not say: the law preview_branch_must_preserve_scroll_contract is violated.

Inspect may say: relevant files include generator/layout/common.py and screen.dart anchors. Inspect may not say: fix common.py by changing scroll host.

Do not emit lawId, repairShape, proposed fixes, implementation steps, targetFiles, or compiler blame language.

Do not inspect figma.png, flutter capture images, compare strips, or heatmaps for visual judgment. Vision belongs to recognise.

Do not perform visual comparison or re-describe user-visible layout from images. May read figma.json metadata or raw.json only when a symptom points to fetch, parse, or source geometry truth.

Repo read is allowed for the first time in the pipeline, but not as unconstrained grep across the whole repo. Follow symptom anchors, inspect_preflight suggestedReads, and repo navigation map hints; expand upstream only as needed.

Repo navigation map in L6 is a navigation aid, not evidence. Do not cite the map as proof of root cause. Use it to choose files to read, then cite actual artifactRefs and repoPaths in entities.

Prefer semantics.json, contract_emit_diff.*, screen.dart, plan.dart, pre_emit.json, processed.json, and dart-errors.json when analyze failed. Use raw.json only when fetch or parse truth is suspected.

Every entity must link to at least one recognise symptom id in relatesToSymptoms. Every entity must include artifactRefs. Each entity must include repoPaths or the step must set blocked=true with missing_evidence for that symptom.

Do not create one entity for the whole screen. One entity represents one compiler concern connected to one or more symptoms.

Use artifactRefs for Figma node paths in processed.json or pre_emit.json when needed for linkage. Do not use node ids as diagnosis keys, branch keys, or screen-specific fix targets in summary text.

On diagnose.refine, do not restart inspect unless the orchestrator explicitly routes inspect.refine. Amend entities only when new evidence is required.

Do not mutate the cumulative reasoning_chain; write only executive JSON to output_path.
