Worktree: {worktree}
Project: {project}
Feature: {feature}
Case mode: {case_mode}
Agent board: {agent_board}
Step: diagnose (3/7)

Debug root: {debug_root}
Manifest: {manifest_path}
Schema: {schema_path}
Output path: {output_path}
Detail log path: {detail_log_path}

Run context:
{run_context_json}

Cumulative reasoning chain (read-only; do not rewrite):
{reasoning_chain_json}

Inspect entities are the WHERE boundary. Read case materials and repo modules only to prove WHY inside those surfaces.

Repo navigation map (deep module slice for inspect entity repoPaths; navigation not evidence):
{repo_map_deep_json}

Do not paste full logs or large artifact bodies into executive JSON.
