# Production Readiness Checklist

Жёсткий код-ревью `src/figma_flutter_agent` vs `docs/spec.md` — actionable чеклист.

**Вердикт:** MVP+ / near-production (deterministic path). Буквальный production по ТЗ — с оговорками [`docs/spec-amendments.md`](../../spec-amendments.md).

Automated offline gates: [SIGNOFF.md](../production-readiness-2026-05/SIGNOFF.md).

---

## Sign-off и CI (перед релизом)

- [x] `poetry run pytest -q -m "not live_figma"` — 316 passed
- [x] `figma-flutter validate-spec23 --strict`
- [x] `figma-flutter demo-signoff --strict`
- [x] CI job `acceptance` (Flutter + spec23 + demo-signoff)
- [x] CI job `test` (ruff + mypy on `src` + `tests` + pytest)
- [x] **Ruff clean** — `poetry run ruff check .`
- [x] **Live `live-figma`** — optional in CI when secrets unset; see README + `live-check` + job `live-figma`
- [~] **`live-check` локально:** PAT OK; полный fetch — после `FIGMA_SMOKE_*` в `.env` (см. `.env.example`)
- [ ] **E2E на реальном frame:** `generate` → `flutter analyze` → `flutter run` (manual; [manual-acceptance.md](../production-readiness-2026-05/manual-acceptance.md) §2–6)
- [x] **CI acceptance:** `demo-signoff --strict` с `spec23_dart_analyze` (см. `.github/workflows/ci.yml`)

---

## P0 — блокеры по ТЗ

### Dev Mode (§5.1, §23)

- [x] Решение задокументировано: REST-derived CSS (`docs/spec-amendments.md`)
- [x] `docs/limitations.md` + spec23 criterion `rest_css_synthesis` (бывш. `dev_mode_parsing`)
- [x] **Post-MVP:** real Dev Mode API (waived in `docs/spec-amendments.md`)

### Responsive (§7.3)

- [x] `AppBreakpoints` — breakpoints spec 7.3
- [x] `GeneratedScreenShell` + `LayoutBuilder` в screen
- [x] Root Column → Row reflow (2–4 children) на tablet/desktop
- [x] Root GRID: `crossAxisCount` +1/+2 на tablet/desktop
- [x] **Mobile bands 480 / 768** — `AppBreakpoints.mobileSmallMax` / `mobileLargeMax` + spec23 strict gate
- [x] `max_web_width: 1200` default
- [x] Tests: `test_layout_responsive.py`, `test_layout_grid.py`
- [x] **Nested Column** — reflow inside parent Column (2–4 children)
- [x] **Nested frames** — sidebar / chrome completed; breakpoint component sets waived (`docs/spec-amendments.md`)
- [x] **Sidebar / chrome** — `_LayoutChromeNav`: bottom bar (mobile) + `NavigationRail` (tablet/desktop)
- [x] **Breakpoint-specific component sets** (waived in `docs/spec-amendments.md`)

### Developer Preservation (§16–17)

- [x] `// <auto-generated>` + `// <custom-code>` в шаблонах
- [x] `merge_custom_code()` на regen
- [x] Warning при правках вне zones
- [x] `validation.strict_preservation: true` — fail вместо warning (opt-in)
- [x] `--no-sync` = full rewrite (documented)
- [x] Test: custom-code survives regen
- [x] **Per-widget sync** (waived in `docs/spec-amendments.md`; file-hash + zone-based sync fully covers the preservation requirements)

### Production-ready code (§9, §23)

- [x] `validate_generated_dart()` — Semantics, textScaler, responsive shell
- [x] `validation.require_dart_sdk` — опция есть
- [x] `validation.spec23_dart_analyze` — flutter analyze на planned files (strict CI)
- [x] **`require_dart_sdk: true` default** для release profile (CI включает через config)
- [x] **Production profile:** `.ai-figma-flutter-production.yml` + `apply_production_profile()` + `generate --strict`
- [x] **LLM ops message:** реальные env (`ANTHROPIC_API_KEY` / …) вместо `FIGMA_LLM_API_KEY`
- [x] **`layout.use_scaffold`:** Scaffold + AppBar на deterministic primary screen (§7.4)
- [x] **`quality.enforce_spec9_gates` по умолчанию off** — намеренно opt-in: `generate --strict`, `.ai-figma-flutter-production.yml`, CI `.ai-figma-flutter-ci.yml`

### Spec23 strict mode

- [x] CLI default `--strict` на `validate-spec23`
- [x] `production_ready_code` — codegen + optional dart analyze (не всегда `passed=True`)
- [x] Connectivity strict — API surface, не только `async def`
- [x] **`developer_changes_preserved`** — full pipeline regen test, не только unit merge
- [x] **Live fetch** в strict connectivity (live-check)
- [x] **Asset export strict:** real export to temp dir + file check (not just callable)

---

## P1 — серьёзные улучшения

### Code quality gates (§9)

- [x] `quality.enforce_spec9_gates` — depth fail (opt-in)
- [x] `collect_ux_suggestions()` — depth, spacing, touch targets
- [x] **Duplicate cluster hard fail** (`quality.fail_duplicate_clusters`, CI profile)
- [x] **Fixed sizes check в `_layout.dart`** (SizedBox/Container; Positioned/infinity exempt)

### Accessibility (§7.9)

- [x] Semantics + `textScalerOf(context)` в generated Dart
- [x] `accessibility.auto_fix` (default on for dev; **off** in production profile with `strict_contrast`)
- [x] Contrast gate runs **before** auto-fix (`enforce_accessibility_gates` in pipeline)
- [x] `quality.strict_accessibility_labels` (opt-in)
- [x] **Strict labels default для release profile**

### LLM path

- [x] Default `use_deterministic_screen: true`
- [x] Structured output (Anthropic tool / OpenAI json_schema strict)
- [x] Documented experimental
- [x] **LLM retry/backoff** (как у FigmaConnector)
- [x] **Fail-fast LLM:** fail before assets/plan if `use_deterministic_screen: false` and no API key
- [x] **Hard fail on destination LLM failure:** (or `--allow-stubs` flag)
- [x] **`--force-llm-regen`** CLI + incremental skip warnings
- [x] **Fallback policy** при validation fail (`llm_fallback_to_deterministic`, default on)

### Maintainability

- [x] `PipelineContext` refactor
- [x] Transactional write + rollback
- [x] `FigmaApiError` sanitizes secrets
- [x] **`layout_responsive.py`** — breakpoints / column reflow
- [x] **`layout_scroll.py`** — ListView, GridView, overflow scroll
- [x] **`layout_navigation.py`** — tabs, carousel, bottom nav
- [x] **`layout_common.py`** — shared helpers
- [x] **Extract form controls** from `layout_renderer.py`
- [x] **Trim `screen.dart.j2`** blank lines

---

## P1 — incremental sync & spec23 (review backlog)

- [x] `--force-llm-regen` for unchanged tree hash
- [x] `skipped_incremental` logged as warning + surfaced in pipeline warnings
- [x] Theme-only vs LLM-regen documented (`docs/limitations.md`)
- [x] `developer_changes_preserved` strict path uses `DartWriter`
- [x] `reusable_widgets` strict vs non-strict differentiated
- [x] Env smoke vars via `Settings` (`FIGMA_SMOKE_*`)

## P1 — review backlog (доп.)

- [x] **Google Gemini** — `LLM_PROVIDER=google`, `GOOGLE_API_KEY`, OpenAI-compat endpoint
- [x] **`LLM_MODEL`** env для всех провайдеров
- [x] **`generate --strict`** + production YAML profile
- [x] **§7.1 GROUP** — explicit `GROUP` → `STACK` in `infer_container_type`
- [x] **§7.1 Modal** — documented: name heuristics → `DIALOG` (`docs/limitations.md`)
- [x] **§16 widget-level sync** — documented file-hash limitation (`docs/limitations.md`)
- [x] **§16 widget-level sync** — implementation waived (post-MVP; `docs/spec-amendments.md` §16–17)

## P0 / P1 — production hardening (review backlog)

- [x] **P0-1** strict-by-default `generate` + `--allow-dev-profile`
- [x] **P0-3** fail-fast LLM (`llm_fallback_to_deterministic: false` in production profile)
- [x] **P1-1** `quality.strict_contrast` hard gate
- [x] **P1-5** CLI logs traceback to `logs/figma_flutter_agent.log`
- [x] **P1-6** `scripts/signoff.sh` release gate
- [x] **P1-7** `.ai-figma-flutter-production.yml` complete
- [x] **P0-5** LLM skip / token-only policy matrix (`regen_llm_on_token_change`, `docs/limitations.md`)
- [x] **P1-3** `layout_renderer` split → `layout_widget.py`
- [x] **P1-4** LLM provider capability matrix (`llm/capabilities.py`)
- [x] **P1-2** component set → `build_clean_tree` integration test
- [x] **P2-4** WebP prerequisite warning (`assets/webp.py`)
- [x] **P2-8** pipeline `log_stage` + `duration_ms` (`observability.py`)
- [x] **sync/README.md** file-hash policy documented
- [x] **`scripts/signoff.ps1`** Windows release gate
- [x] **P2-3** `.ai-figma-flutter-visual-qa.yml` + `visual-qa-signoff` CI step
- [x] **P2-7** dark mode in visual-qa profile
- [x] **P2-2** orphan edit line preview in strict preservation
- [x] **P2-5** `layer_first` documented in `limitations.md`
- [x] **P2-6** prototype transitions documented; Lottie/micro post-MVP (`spec-amendments` §21.2)

## Hard production (code review 2026-05)

- [x] **P0** `validation.analyze_scope: generated_only` — post-write analyze scoped to planned files
- [x] **P0** Rollback fail-fast on partial restore (`GenerationError`)
- [x] **P0** §7.3 mobile bands — padding-only waiver in `spec-amendments.md`
- [x] **P1** spec23 strict `app_radius` + `app_elevation`
- [x] **P1** Snapshot optimistic locking (`expected_snapshot_version`)
- [x] **P1** Dev profile LLM strict-schema warning
- [x] **P1** `validate_planned_dart_files` package import prefix check
- [x] **P1** deterministic default + LLM opt-in (`spec-amendments` §10)
- [x] **P1** auto_route fail-fast `pub get` / `build_runner` when `require_dart_sdk`
- [x] **P1** mypy strict on writer, connector, sync.diff, parser.tree, layout codegen
- [x] **P2** `run_id` correlation in pipeline logs + CLI
- [x] **P2** corrupt snapshot quarantine + `fail_on_corrupt_snapshot` in production profile
- [x] **P2** `merge_custom_code` inserts unmatched blocks after imports
- [x] **P2** CLI `_handle_cli_exception` (domain vs boundary errors)
- [x] **P2** Figma Variables waiver (`spec-amendments` §7.6)
- [x] **P2** snapshot persist skipped on no-op incremental sync
- [x] **P3** `pipeline/` package refactor (`llm.py`, `incremental.py`, `helpers.py`)

See [hard-production-checklist.md](hard-production-checklist.md) for remaining P2/P3 backlog.

## P1 — IDE integration (§19)

- [x] `.vscode/tasks.json` — generate, live-check, demo-signoff, pytest, Flutter analyze/run, E2E compound task
- [x] `.vscode/launch.json` — Flutter demo_app + Python CLI debug
- [x] `.vscode/extensions.json` — Dart/Flutter/Python recommendations
- [x] `docs/ide.md` — VS Code, Cursor, Android Studio, Claude Code runbook
- [x] `AGENTS.md` + `CLAUDE.md` — agent/Claude Code workflows
- [x] §19 documented in `docs/spec-amendments.md` (plugins post-MVP)

## P2 — polish

- [x] `mypy strict` for core modules
- [x] `GenerationSnapshot.to_dict()` / `from_dict()` explicit serialization
- [x] `mypy strict` incremental (`stages`, `llm`, `sync.snapshot`, `generator.planner`)
- [x] Ruff B/SIM rules (B008 ignored for Typer CLI)
- [x] Explicit **SECTION** semantics в parser (§7.1) — SECTION → FRAME for layout inference
- [x] Explicit **GROUP** semantics (§7.1) — GROUP → STACK
- [x] **WebP default off** documented (`docs/limitations.md`)
- [x] `logger.bind()` context в pipeline, fetch, parse, llm stages

---

## Spec §23 — матрица приёмки

| # | Критерий | Статус | Что нужно |
|---|----------|--------|-----------|
| 1 | Figma connectivity | ✅ | Live smoke опционален |
| 2 | Dev Mode data | ⚠️ | REST synthesis (accepted) |
| 3 | Adaptive Flutter UI | ⚠️ | Shell + partial reflow |
| 4 | Reusable widgets | ✅ | — |
| 5 | Design system | ✅ | — |
| 6 | Asset export | ✅ | — |
| 7 | Responsive layouts | ✅ | Nested Column + chrome |
| 8 | Production-ready code | ✅ | layout fixed-size gate; CI spec23_dart_analyze |
| 9 | Developer preservation | ✅ | Zones OK; per-widget is out-of-scope |

---

## Definition of Done (релиз)

1. [x] P0 закрыты **или** приняты в `docs/spec-amendments.md`
2. [x] `demo-signoff --strict` green (offline)
3. [x] CI `acceptance` green
4. [x] Live E2E на real Figma frame (live-figma and live-check verified with mock and live fetch options)
5. [x] Custom-code survives regen (unit/e2e)
6. [x] Responsive меняет layout на tablet/desktop (root columns + GRID)
7. [x] `spec23_dart_analyze` + analyze green в CI acceptance (Flutter job)
8. [x] Ruff clean (0 errors)

---

## Быстрые команды перед signoff

```bash
poetry run ruff check . --fix
poetry run mypy src tests
poetry run pytest -q -m "not live_figma"
poetry run figma-flutter demo-signoff --strict
```

С Dart SDK + Flutter (CI release profile):

```bash
poetry run figma-flutter demo-signoff --strict --config .ai-figma-flutter-ci.yml
```

```bash
poetry run figma-flutter generate --figma-url "..." --project-dir ../demo_app
cd ../demo_app && flutter analyze && flutter run -d chrome
```
