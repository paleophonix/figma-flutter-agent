# Core audit — source reports

Place verbatim copies of the three parallel audits here so §4 «Источник» columns trace to full text.

| File (suggested) | Label | Notes |
|------------------|-------|--------|
| `audit-A-internal.md` | `A` | Anti-patching, path divergence, mutations |
| `audit-B-contracts.md` | `B` | IR vs deterministic matrix, AST policy skips |
| `audit-C-geometry.md` | `C` | Geometry, transforms, SVG fit (see §3 corrections) |

Do not edit findings in place when the code changes — add a dated addendum at the top instead.
